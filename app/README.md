# Medallion Visual Interface

Run the pipeline first:

```bash
python -m src.pipeline.run_medallion
```

Then open:

```text
app/medallion_viewer.html
```

The HTML file is generated from `data/gold/medallion_summary.json` and shows the current raw, bronze, silver, and separate gold outputs for listings and reviews.
